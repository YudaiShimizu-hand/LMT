<div class="row">
<div class="col">
    <% if @user.user_detail.nil? %>
            <img src="/assets/user-image.png" alt="">
    <% else %>
        <%= @user.user_detail.image %>
    <% end %>
    <table>
    <tr>
        <th>ユーザー名</th>
    </tr>
    <tr>
        <td><%= @user.username %></td>
    </tr>
    <tr>
        <th>メールアドレス</th>
    </tr>
    <tr>
        <td><%= @user.email %></td>
    </tr>
    <tr>
        <th>会社名</th>
    </tr>
    <tr>
    <% if @user.user_detail.nil? %>
         <td>記載なし</td>
    <% else %>
        <td><%= @user.user_detail.company_name %></td>
    <% end %>
    </tr>
    </table>
</div>
<div class="col"></div>
</div>